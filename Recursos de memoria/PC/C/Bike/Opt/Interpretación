⏱️ Tiempo

User time (seconds): 21.16 → el programa estuvo usando CPU en modo usuario ~21 s.

System time (seconds): 0.00 → casi nada de tiempo en llamadas al kernel.

Elapsed (wall clock): 21.18 s → el tiempo de reloj coincide con el tiempo de CPU → significa que tu programa se ejecutó en un solo core sin bloqueos.

Percent of CPU: 99% → confirma lo anterior: la CPU estuvo ocupada casi al 100% durante toda la ejecución.

👉 En resumen: la versión optimizada tarda ~21 s en ejecutar tus pruebas, consumiendo un núcleo completo.

💾 Memoria

Maximum resident set size: 6164 kB (~6 MB) → la memoria máxima usada sigue siendo muy baja, idéntica a la versión reference.

No hubo Swaps, por lo que no se saturó la RAM.

👉 La optimización no cambia el consumo de memoria, sigue en torno a los 6 MB.

⚙️ Entrada/Salida

File system inputs: 80 / outputs: 72 → actividad de disco muy baja, solo guardado de resultados (ej. bike_rendimiento.csv).

Major page faults: 3 → muy pocos accesos que requirieron leer desde disco.

Minor page faults: 290 → normales, se deben a carga de memoria en RAM.

🔄 Planificación

Voluntary context switches: 2 → prácticamente nunca cedió la CPU voluntariamente.

Involuntary context switches: 141 → fue interrumpido un poco por el sistema operativo, pero nada grave.

✅ Conclusión para test_bike en optimized:

Tiempo total ≈ 21 s (similar a reference, donde tuviste ~20 s).

CPU al 99% todo el tiempo → ejecución eficiente.

Memoria estable y baja (~6 MB).

E/S mínima → el cuello de botella no es el disco.

📊 Si comparas reference vs optimized:

Reference: 20.25 s, 6 MB, 99% CPU.

Optimized: 21.18 s, 6 MB, 99% CPU.

👉 En tu caso, la versión optimized no ha dado mejoras en tiempo ni memoria, al contrario, salió ligeramente más lenta (casi 1 s más), pero dentro del margen de variación normal (puede deberse al ruido del sistema, context switches, etc.).
