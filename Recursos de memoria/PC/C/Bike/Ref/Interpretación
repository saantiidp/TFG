⏱️ Tiempo

User time (seconds): 20.23 → tiempo que la CPU pasó ejecutando instrucciones de tu programa (modo usuario).

System time (seconds): 0.00 → tiempo que la CPU pasó en llamadas al kernel (operaciones de sistema, E/S, etc.).

Elapsed (wall clock) time: 0:20.25 → tiempo total “de reloj”, lo que tardó el programa en ejecutarse.

👉 Como User + System ≈ Elapsed y el Percent of CPU es 99%, significa que tu programa usó prácticamente un core completo durante toda la ejecución.

💾 Memoria

Maximum resident set size (kbytes): 6164 → la memoria máxima que tu proceso ocupó en RAM fue ~6 MB.
🔎 Este es el dato más relevante para ti si quieres saber el consumo máximo de memoria.

⚙️ E/S y memoria virtual

Major page faults: 19 → veces que el proceso necesitó leer páginas desde disco (relativamente bajo).

Minor page faults: 283 → veces que el proceso tuvo que mapear páginas de memoria ya en RAM.

File system inputs: 2448, outputs: 80 → cantidad de accesos a disco (lecturas y escrituras).

Swaps: 0 → nunca necesitó usar swap, buena señal.

🔄 Planificación

Voluntary context switches: 15 → veces que tu programa cedió la CPU voluntariamente (espera activa o bloqueos).

Involuntary context switches: 120 → veces que el sistema operativo lo interrumpió para dar CPU a otro proceso.

✅ Conclusión para test_bike:

Ejecutó durante ~20 s con un uso de CPU prácticamente completo (99% de un núcleo).

El consumo máximo de memoria fue muy bajo (~6 MB).

No usó swap, por lo que la memoria fue más que suficiente.

Hubo cierta actividad de E/S (2448 lecturas), seguramente por ficheros temporales o inicialización.
